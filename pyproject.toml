[project]
name = "mahjoub-online"
version = "0.1.0"
dependencies = []

[tool.vercel]
entrypoint = "api/index.py"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
